export type Book = {
    bookId: string
    category: string
    title: string
    author: string
    description: string
    price: number
    image: string
    quantity: number
    isbn: string
    publishedAt: string
    createdAt: string
    updatedAt: string    
  }